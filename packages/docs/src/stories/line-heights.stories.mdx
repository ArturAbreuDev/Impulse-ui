import { Meta, Description } from '@storybook/addon-docs/blocks';
import {TokensGrid} from '../components/TokensGrid.tsx'
import {lineHeights} from '@impulse-ui/tokens'

<Meta title="Token/LineHeights" component={'Content Page'} />

# Line Heights

Esses são os espaçamentos entre as linhas utilizadas nesse Design System

<TokensGrid tokens={lineHeights} />


